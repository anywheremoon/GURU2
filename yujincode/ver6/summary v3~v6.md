## ✅ v3~v6구현 완료 기능 요약

1. 사진 등록 기능

2. 복용 통계 기능

3. 영양제 조합 추천 페이지 구현
>  약 조합&약 종류 데이터는 JSON 파일로 관리, 추후 JSON 데이터 확장하면 더 좋을 듯
> 공공데이터 활용 가산점 해당할지는 애매,,

4. 오늘 목록 외에도 다른 날도 체크 가능하도록 구현
> 오늘 스케줄 페이지에서 날짜 클릭 시 다른 날짜로 전환 가능

5. 하단 액션바(Navigation Bar)

6. 달력 진입 시 오늘 날짜 기본 선택

7. 복용 데이터 초기화 기능 구현

8. 복용 기간 설정

9. 시작일 / 종료일 달력으로 선택
> 종료일 미지정 옵션(체크박스)
> 시작일 > 종료일 설정 불가 처리

❌구현 안 된 것
1. 알람
2. 인트로 -> 기존 인트로가 이미 있어서, 2번 인트로 만들기보단 기존 인트로를 수정하는 편이 나을 것 같음. (노션 2차회의록 맨 아래 사진 참고)


--------
(참고)
📁 JSON 데이터 파일 생성 방법
STEP 1. assets 폴더 생성

Android Studio 좌측 Project 창

상단 드롭다운을 Android → Project로 변경

경로 이동

app > src > main


main 폴더 우클릭
→ New → Folder → Assets Folder → Finish

📂 생성 결과

app/src/main/assets/

STEP 2. JSON 파일 생성

assets 폴더 우클릭

New → File

파일명 입력

supplements.json
